node_modules
dist
*.local
.claude/settings.local.json
.DS_Store
worker/node_modules
worker/.wrangler

# Never commit local secrets — this repo is public
.env
.env.*
# Cross-device sync code: whoever holds it can read and overwrite the progress bucket
ticourse-sync-code.txt
